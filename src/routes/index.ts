import { Home } from '../pages'
/*
Add new route:
Ex: 
import {Login} from '../pages'
{ path: '/login', element: Login }
*/

const publicRoutes = [
     { path: '/', element: Home }
]

export { publicRoutes }